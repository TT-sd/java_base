一、Ioc
概念：就是将生成（new）对象的控制权由内部交给外部容器创建的重要思想

说明：
1.spring提供了一个Ioc容器，可用来充当外部的容器。
2.内部只负责对象的声明，而Ioc来创建和初始化对象。
3.交给ioc管理的对象称为Bean对象。

二、Di

依赖注入，是对IOC思想的实现。
说明：
1.将ioc容器的bean自动注入到需要的类里


三、适合交给容器管理的bean
说明：是一些可以复用的对象，不需要用一次创建一个的对象，
表现层、业务层、数据层、工具对象

四、不能交给容器管理的bean
是一些实体类对象，他们通常每次创建的对象的属性值都不太一样

